# ──────────────────────────────────────
# Varni Packaging — Dashboard Environment
# ──────────────────────────────────────

# Backend API URL (OPTIONAL in dev, REQUIRED in production)
# In local development, the Vite proxy handles /api → localhost:3000
# In production (Railway), set to your backend URL:
# VITE_API_URL=https://varni-api.up.railway.app
